<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.datawizard">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DataWizard">
        <activity
            android:name=".SummaryActivity"
            android:parentActivityName=".DateOfBirthActivity" />
        <activity
            android:name=".DateOfBirthActivity"
            android:launchMode="singleTop"
            android:parentActivityName=".AddressActivity" />
        <activity
            android:name=".AddressActivity"
            android:launchMode="singleTop"
            android:parentActivityName=".NameActivity" />
        <activity
            android:name=".NameActivity"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>